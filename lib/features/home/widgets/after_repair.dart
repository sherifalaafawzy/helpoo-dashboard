import 'package:flutter/material.dart';

class AfterRepairTab extends StatelessWidget {
  const AfterRepairTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('After Repair'),
    );
  }
}
