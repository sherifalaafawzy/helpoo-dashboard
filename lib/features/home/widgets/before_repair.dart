import 'package:flutter/material.dart';

class BeforeRepair extends StatelessWidget {
  const BeforeRepair({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Before Repair'),
    );
  }
}
