import 'package:flutter/material.dart';

class FNOL extends StatelessWidget {
  const FNOL({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FNOL'),
    );
  }
}
